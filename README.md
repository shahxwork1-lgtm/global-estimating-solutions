# Global Estimating Solutions

Website/project files prepared for GitHub.

## Important
- Do not upload `node_modules` or `.env` files.
- Keep API keys, passwords, and private credentials out of GitHub.
- If this is a web project, install dependencies locally and use the project's existing package scripts after cloning.
